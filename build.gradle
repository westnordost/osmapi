subprojects {
    apply plugin: 'java'

    repositories {
        mavenLocal()
        mavenCentral()
    }

    group = "de.westnordost"

    sourceCompatibility = 1.7
    targetCompatibility = 1.7
}

ext {
    all_version = 3.10
    core_version = 1.4
    changesets_version = 1.5
    user_version = 1.4
    traces_version = 1.4
    notes_version = 1.4
    map_version = 1.5
}