subprojects {
    apply plugin: 'java'

    repositories {
        mavenLocal()
        mavenCentral()
    }

    group = "de.westnordost"

    sourceCompatibility = 1.7
    targetCompatibility = 1.7
}

ext {
    all_version = 3.8
    core_version = 1.2
    changesets_version = 1.3
    user_version = 1.2
    traces_version = 1.2
    notes_version = 1.2
    map_version = 1.3
}