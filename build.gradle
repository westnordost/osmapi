subprojects {
    apply plugin: 'java'

    repositories {
        mavenLocal()
        mavenCentral()
    }

    group = "de.westnordost"

    sourceCompatibility = 1.7
    targetCompatibility = 1.7
}

ext {
    all_version = 3.9
    core_version = 1.3
    changesets_version = 1.4
    user_version = 1.3
    traces_version = 1.3
    notes_version = 1.3
    map_version = 1.4
}